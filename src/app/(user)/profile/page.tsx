"use client";

import ProfileTabs from "./_components/tabs/Tabs";

type Props = object;

const page = (props: Props) => {
  return (
    <div>
      <ProfileTabs />
    </div>
  );
};

export default page;
